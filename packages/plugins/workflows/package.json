{
  "name": "@nakama/plugin-workflows",
  "version": "1.0.1",
  "type": "module",
  "files": [
    "nakama.plugin.json",
    "actions",
    "migrations",
    "ui",
    "skills"
  ],
  "scripts": {
    "build": "bun build ./src/actions.ts --outdir actions --target bun && bun build ./src/ui.tsx --outfile ui/app.js --target browser",
    "test": "bun test src"
  },
  "devDependencies": {
    "@nakama/core": "workspace:*",
    "@nakama/ui": "workspace:*",
    "@types/react": "^19.3.0"
  }
}
